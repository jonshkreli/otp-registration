import {createTheme} from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "inherit",
  },
  palette: {
    primary: {
      main: "#1E3A34",
    },
    background: {
      default: "#F8F8F8",
    },
  },
});

export default theme;
